# reedhold-mesh

Peer discovery, daily relay lottery, frames, and an in-process fabric.

Transitional sync hosts are ordinary peers redrawn every epoch. The
optional company site is never required. UDP, libp2p, Freenet, and BLE
plug in later as `Link` implementations; routing does not change.
