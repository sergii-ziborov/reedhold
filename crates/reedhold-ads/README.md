# reedhold-ads

The company / founder key can issue short-lived advertising-operator
certificates. It cannot decrypt messages, sign as a user, halt the
mesh, or rewrite history.

The sandbox market is a batch uniform-price auction on
`(topic, reputation bucket, epoch)`. Inventory is never sold by user id.
Losing the genesis key does not halt clearing. Credits are not real money.
