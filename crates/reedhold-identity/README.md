# reedhold-identity

A Reedhold account is a random `MasterSeed`. A password never creates
the identity. Device keys are derived separately so a phone and a laptop
do not share one append-only sequence.
